# Future microservice scaffolds (not used by MVP)

The active backend is `../platform-api/`. These services are archived for a future microservices migration.

Moved from `services/` on refactor — enable via `docker compose --profile services up -d` after updating paths in `infrastructure/docker/services.yml`.
